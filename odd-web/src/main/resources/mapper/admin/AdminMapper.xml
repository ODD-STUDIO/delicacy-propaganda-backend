<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.odd.delicacy.mapper.admin.AdminMapper">

    <resultMap id="AdministerVO" type="Administer">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectAdmin">
        SELECT a.id,a.username,a.name,a.create_time,a.update_time,a.password

        FROM administer a
    </sql>

    <select id="findById" parameterType="long" resultMap="AdministerVO">
        <include refid="selectAdmin"/>
        WHERE a.id = #{id} AND a.is_deleted = 0
    </select>

    <select id="findByUsername" parameterType="String" resultMap="AdministerVO">
        <include refid="selectAdmin"/>
        WHERE a.username = #{username} AND a.is_deleted = 0
    </select>

    <insert id="insert" parameterType="administer">
        INSERT INTO administer(
               create_time, update_time, username, password, name
        ) VALUES ( now(),now(),#{username},#{password},#{name})
    </insert>

    <update id="update" parameterType="Administer">
        UPDATE administer
        <set>
            <if test="username!=null and username!=''">
                username = #{username}
            </if>
            <if test="password!=null and password !=''">
                password = #{password}
            </if>
            <if test="name != null and name != ''">
                name = #{name}
            </if>
            update_time = now()
        </set>
        WHERE id = #{id}
    </update>
</mapper>